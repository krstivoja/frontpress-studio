{% extends '_layout.twig' %}
{% set page_title = meta.title|default('Page') %}

{% block content %}
<article>
  <h1 class="page-title">{{ meta.title|default('') }}</h1>
  {{ html|raw }}
</article>
{% endblock %}
